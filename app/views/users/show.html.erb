<h1><%=@user.username%>'s Profile</h1>
<%if @user.friends.include?(current_user)%>
You're friends!<br>
<%elsif @user != current_user && @user.friends.exclude?(current_user)%>
<p><%=link_to "Become Friends", friendships_path(friend_id: @user.id), method: :post %><p>
<%end%>


<li>Name: <%=@user.to_s%></li>
<li>Email: <%=@user.email%></li>
<%if%>
<%current_user==@user%>
<br/>
<%=link_to "Update Account Info", edit_user_path(@user)%><br/>
 <!-- but how do i restrict someone from going staight to the url -->

<br/><br/>

  <!-- 3 scenarios - in order below
  - this is me; option to update info; see dashboard
  - this is my friend; "you're friends"
  - this is a 'stranger'; option to become friends -->


 <h3>Dashboard</h2>
  <p>You have:</p>
  <table cellspacing="10">
    <col width="130">
    <col width="130">
    <col width="130">
    <tr>
      <th>Upcoming Hangouts</th>
      <th>Invitations</th>
      <th>Friends</th>
    </tr>
    <tr>
    <th><%=link_to @user.hangouts_count, hangouts_path%></th>
    <th><%=link_to @user.invitations_count, hangouts_path%></th>
    <th><%=link_to @user.friends_count, users_path%></th>
    </tr>
  </table>
  <%end%>


  <br/><br/><br/><br/><br/>
